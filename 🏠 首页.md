---
draft: false
---

# 🏠 知识库首页

> 全维度个人知识体系。Obsidian 管存储与图谱，GitHub 私有仓同步，Codex/Claude 按 `README.md` 调用规则读取。
> 投研为首个领域，后续扩展技术学习、读书、项目追踪等。

## 🚀 快速入口

> 5 个最常用入口——一键直达。

| 入口 | 链接 | 说明 |
|---|---|---|
| 📊 投研 MOC | [[10_Reference/investing/MOC]] | 投研知识图谱入口（2400+ 实体） |
| 🌡️ 市场情绪 | [[10_Reference/market_sentiment/DASHBOARD]] | 每日 pre/mid/post 报告 + z-score 温度 |
| 🔍 审查报告 | [[10_Reference/investing/reviews/index]] | ReAct Agent 体检报告（8 项检查） |
| 🌐 Quartz 站点 | [lzw9560.github.io/knowledge](https://lzw9560.github.io/knowledge) | 线上知识库站点 |
| 🤖 飞书 Bot | [[10_Reference/investing/specs/archive/m7-卡片对齐/S101-飞书多点通知]] | 飞书多点通知决策 |

---

## 📊 图谱统计实时面板

### 投研子区实体总数

```dataview
TABLE WITHOUT ID
  length(rows) AS "实体总数"
FROM "10_Reference/investing"
WHERE type != null AND file.name != "index" AND file.name != "MOC" AND file.name != "README"
```

### 各类型计数

```dataview
TABLE length(rows) AS "数量"
FROM "10_Reference/investing"
WHERE type != null AND file.name != "index" AND file.name != "MOC" AND file.name != "README"
GROUP BY type
SORT length(rows) DESC
```

---

## 🕐 最近活动

> 最近 7 天修改的文件——追踪知识图谱最新变化。

```dataview
TABLE WITHOUT ID
  file.link AS "文件",
  type AS "类型",
  dateformat(file.mtime, "MM-dd HH:mm") AS "修改时间"
FROM "10_Reference/investing"
WHERE type != null AND file.mtime >= date(today) - dur(7 days)
SORT file.mtime DESC
LIMIT 20
```

---

## 三层架构

```
00_Active/      ← 活跃层：当前项目、近期决策、沟通记录
10_Reference/   ← 参考层：模板、索引、跨项目知识（含投研知识图谱）
20_Archive/     ← 归档层：已结束项目、周复盘压缩包
```

## 领域导航

| 领域 | 入口 | 状态 | 说明 |
|---|---|---|---|
| 📊 投研 | [[10_Reference/investing/MOC]] | ✅ 已建 | 16 实体类 + 16 模板 + 12 战法卡 + 2400+ 实体 |
| 🌡️ 市场情绪 | [[10_Reference/market_sentiment/DASHBOARD]] | ✅ 运行中 | 每日 pre/mid/post 报告 + SQLite 数据管道 |
| 💻 技术学习 | [[10_Reference/tech-learning/MOC]] | ✅ 已建 | 5 实体类 + 3 模板 + 7 种子实体（语言/框架/工具/架构） |
| 📚 读书/学习 | [[10_Reference/reading/MOC]] | ✅ 已建 | 3 实体类 + 3 种子实体（书单/笔记/摘抄） |
| 📋 项目追踪 | [[10_Reference/projects/MOC]] | ✅ 已建 | 3 实体类 + 3 种子实体（进行中/候选/归档） |

## 索引文件

- [[00_Active/INDEX]] — 活跃层索引（每次任务先读这个）
- [[10_Reference/index/MASTER_INDEX]] — 跨项目全局索引
- [[10_Reference/investing/MOC]] — 投研知识图谱入口

## 全局统计

```dataview
TABLE length(rows) AS "笔记数"
FROM ""
WHERE file.name != "首页"
GROUP BY file.folder
SORT file.folder ASC
```

## 同步

- GitHub 仓：`lzw9560/knowledge`（私有）
- 本地路径：`/Users/lizhiwei/Documents/Obsidian Vault`
- `.gitignore` 排除 Obsidian 个人状态（workspace.json/cache/.trash）和 Claude-Memory 软链目标
- 已装 `obsidian-git` 插件，可在 Obsidian 内直接 commit/push

## 使用指南

### 新建投研实体
1. 在 `10_Reference/investing/` 对应文件夹下新建笔记
2. 用 Templater 应用模板（模板在 `10_Reference/investing/templates/`）
3. 用 `[[]]` 建立关系链接（如股票 → 行业 → 研报）

### 跨领域链接
在某领域笔记里直接 `[[10_Reference/investing/stocks/600519]]` 跨域引用。

### 新领域上线步骤
1. 在 `10_Reference/` 下建领域文件夹（如 `10_Reference/tech-learning/`）
2. 建 MOC 入口
3. 建 templates/ 子目录
4. 回首页更新领域导航表

## 调用规则（Codex/Claude 必须遵守）

1. **先读索引** — 每次任务开始，只读 `00_Active/INDEX.md` 和 `10_Reference/index/MASTER_INDEX.md`
2. **按需调取** — 根据索引定位到具体项目文件夹，只读该项目的 `SUMMARY.md` 和最新更新文件
3. **禁止全库加载** — 不得一次性读取全库内容
4. **写回规范** — 新决策、沟通记录写入 `00_Active/` 对应文件夹，更新项目 `SUMMARY.md` 的「最后更新」字段
5. **归档由定时任务处理** — Codex 不手动归档，由凌晨 2 点定时任务自动整理
