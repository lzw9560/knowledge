---
type: logic
rule_id: RENAME-001
rule_type: 自动化规则
target_entity: 全类型
severity: high
condition: 实体改名（文件移动/重命名）→ 用 move_note 重写所有反向链接
action_on_violation: 阻止 agent 直接文件系统 rename；强制走 move_note
source: ora-3 诊断 §6.5（move_note 危险操作约束）+ obsidian-mcp-setup.md
created: 2026-09-07
confidence: high
---

> [!info] ⚙️ 规则
> **规则**：`RENAME-001`  **类型**：自动化规则
> **严重级**：high  **适用实体**：全类型
>
> **违反处置**：`阻止 agent 直接文件系统 rename；强制走 move_note`

## 📋 规则定义

- 类型：`自动化规则`
- 适用实体：`全类型`
- 严重级：`high`


## ⚡ 触发条件

实体改名场景：
- 股票改名（如 `stocks/600519.md` 重命名为 `stocks/maotai.md`）—— **不推荐**，股票应以代码为键
- spec 重新编号（如 S006 并入 S007，文件重命名）
- 文件夹结构调整（实体迁移）


## 🔧 执行逻辑

```
触发：实体改名请求
  1. 校验调用方：
     if agent 调用:
       拒绝（ora-3 §6.5：move_note 只对人工开放）
     if 人工调用:
       继续
  2. 执行 move_note(old_path, new_path):
     - 移动/重命名文件
     - 扫描全库 wikilink，把指向 old_path 的链接重写为 new_path
     - 处理别名（old|alias → new|alias）
  3. 记录改名日志（who/when/old/new）
```


## ⚠️ 违反处置

- `action_on_violation`：阻止 agent 直接文件系统 rename
- 若检测到文件被外部 rename（git diff 显示文件移动但反向链接未更新）→ 审查脚本报 broken_link（反向链接指向旧路径）


## 🔗 关联

- **触发动作**：[[actions/]]
- **约束实体**：[[stocks/]]
- **来源决策**：[[specs/]]
- **出链**：`=(length(this.file.outlinks))` 个 · **入链**：`=(length(this.file.inlinks))` 个
