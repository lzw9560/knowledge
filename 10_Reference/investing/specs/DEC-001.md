---
type: decision
number: DEC-001
title: 候选池漏斗性能优化技术路线
status: 已采纳
created: 2026-09-06
---

# DEC-001 候选池漏斗性能优化路线

## 选择

采用「漏斗级缓存 + 盘后预计算 + top-N 限界 + 独立 source 并行」组合方案优化 `run_funnel` 性能；**不**对 `fund_flow`/`catalyst` 做逐只并发。

## 理由

`astock.em_get` 有全局串行限流锁（QPS≤2）。`fund_flow` 逐只调用都走 em_get（每只 ~2s × ~100 只 ≈ 200s，为 >60s 主因）；逐只并行在锁约束下退化为串行，**无收益**且并发请求放大东财封 IP 风险。故性能杠杆排序为：缓存+预计算（主杠杆，把 ~200s 冷算挪到盘后离线，请求侧恒 ≤1s）> top-N 限界（r1_kept ~100→≤80，降预计算墙钟且封顶）> 独立 source 并行（仅对非 em_get 串行的 gene/board/auction 有真收益）。

## 被否决方案

1. **fund_flow 逐只并发**：em_get 全局锁使并发退化为串行，反而放大封 IP 风险，收益为负。
2. **`run_funnel` 改 async 逐 source `to_thread`**：改动面大，source 内部仍串行 em_get，收益不如缓存+预计算；且 S003 已在路由层做 `asyncio.to_thread`，重复。

## 关联

- 相关 spec：[[specs/S004-candidates-funnel-performance]]（草案，待审批） / [[specs/S008-后端数据层迁移]]（astock 门面化） / [[specs/S031-调度收口盘前多层按战法回测]]（漏斗三层）
- 影响数据源：[[data-sources/eastmoney-push2]] [[data-sources/akshare]]（em_get 防封红线）
- 影响实体：[[stocks/]]（候选池）
- 源文件：`specs/decision-log.md`
